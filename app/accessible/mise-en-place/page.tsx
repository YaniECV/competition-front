import { Suspense } from "react";
import { AccessibleMiseEnPlace } from "@/components/pages/Preparer";

export default function Page() {
  return (
    <Suspense>
      <AccessibleMiseEnPlace />
    </Suspense>
  );
}
