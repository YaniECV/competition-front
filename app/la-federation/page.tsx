import type { Metadata } from "next";
import { LaFederation } from "@/components/pages/Federation";

export const metadata: Metadata = {
  title: "La fédération — FEST_ACCESS",
};

export default LaFederation;
