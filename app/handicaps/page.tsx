import type { Metadata } from "next";
import { HandicapsIndex } from "@/components/pages/Handicaps";

export const metadata: Metadata = {
  title: "Les handicaps — FEST_ACCESS",
};

export default HandicapsIndex;
